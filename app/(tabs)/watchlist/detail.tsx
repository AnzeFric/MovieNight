import CustomText from "@/components/global/CustomText";
import { View } from "react-native";

export default function DetailMovie() {
  return (
    <View>
      <CustomText type={"normal"}>Detail movie screen</CustomText>
    </View>
  );
}
